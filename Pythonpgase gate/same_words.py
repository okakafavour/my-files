def get_if_the_two_string_are_same(value:str)->bool:
	for char in values:
		if char == values:
			if char != values:
				return False
		return True

char = "silent"
values = "listen"

filter = values == values

print(get_if_the_two_string_are_same(filter))